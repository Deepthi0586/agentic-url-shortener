package com.saigangili.orchestrator.cli;

/**
 * Entry point for the orchestrator CLI.
 *
 * This will load a requirement (from a file or argument), build the stage
 * graph, and run it — pausing at approval gates for human input.
 *
 * Currently a placeholder; wired up once the stage graph (Phase 1) and
 * core engine (Phase 2) are in place.
 */
public class OrchestratorCli {

    public static void main(String[] args) {
        System.out.println("Orchestrator CLI — scaffold OK. Stage graph not yet wired up.");
    }
}
